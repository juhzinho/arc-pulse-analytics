export const WINDOW_OPTIONS = ["1h", "6h", "24h", "7d"] as const;
export const MARKET_STATUS = ["open", "resolved", "cancelled"] as const;
export const MARKET_METRICS = ["tps", "gas_total", "block_time", "top_contract"] as const;
export const MARKET_TYPES = ["binary", "range"] as const;
export const USER_ROLES = ["USER", "ADMIN"] as const;
